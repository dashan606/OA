package com.ssm.OaManager.mailfun;



import javax.mail.MessagingException;
import javax.mail.Session;

import com.ssm.OaManager.entity.mail.UserAuthenticator;

/**
 * 
 * @author TH
 *
 */

public class MailLogin {
	
	/*static SendMail sendMail = new SendMail();*/
	
	
	/*//判断登录
	public static Session isLogin(UserAuthenticator userAuth,String keyword){
		
	    
	    	//连接服务器
			Session session=null;
			try {
				session = sendMail.connectSMTP(userAuth, keyword);
			} catch (MessagingException e) {
				e.printStackTrace();
			}
			
			return session;
		
	}*/
	
	      
}
